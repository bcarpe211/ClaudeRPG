# Sequence 8 canary migration

This directory is deliberately excluded from the public companion quartet. It
contains the one-time transaction used only to migrate the single verified
sequence-8 canary from its exact legacy layout. The normal installer must not
invoke or embed it.
