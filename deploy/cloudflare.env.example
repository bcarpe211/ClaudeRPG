# Cloudflare API token with Zone:DNS:Edit permission on the redlattice.com zone.
# Caddy uses it for the ACME DNS-01 challenge. Real file: /etc/caddy/cloudflare.env
# (chmod 600, root-owned). Do NOT commit the real token.
CLOUDFLARE_API_TOKEN=replace-with-your-cloudflare-token
